/**
 * Function that parses the query parameters of an url, returns both value
 * the url and the query parameters.
 * @param {String} url
 * @return {Array<String, object>}
 */
declare const querystring: (url: string) => any[];
export { querystring };
